name: Test Ubicloud Prod Runners

on:
  push:
    branches: [test-dockercompose]
 
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        runs-on: [ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud]
    runs-on: ${{ matrix.runs-on }}
    steps:
      - name: checkout
        uses: actions/checkout@v4

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3
  
      # - name: bake
      #   uses: docker/bake-action@v5
      #   with:
      #     load: true
      #     set: |
      #       *.cache-to=type=gha,mode=max                                                                                
      #       *.cache-from=type=gha                                                                                       

      - run: docker compose -f ./docker-compose.yml up --build -d
 