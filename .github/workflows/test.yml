name: Test Ubicloud Prod Runners

on:
  push:
    branches: [check-dns]
 
jobs:
  test:
    strategy:
      # max-parallel: 1
      matrix:
        runs-on: [ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud, ubicloud]
        # runs-on: [ubicloud]
    runs-on: ${{ matrix.runs-on }}
    steps:
      - name: checkout
        uses: actions/checkout@v4

      # - name: resolvectl status
      #   run: resolvectl status

      # - name: change DNS provider from Quad9 to Cloudflare
      #   run: |
      #     sudo mkdir -p /etc/systemd/resolved.conf.d
      #     echo "[Resolve]
      #     DNS=9.9.9.9 149.112.112.112 2620:fe::fe 2620:fe::9" | sudo tee /etc/systemd/resolved.conf.d/Ubicloud.conf
      #     sudo systemctl restart systemd-resolved.service

      # - name: resolvectl status
      #   run: resolvectl status

      - name: docker build
        run: docker build -t example .
 
        

